# ── macOS ──
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# ── Private / project-local (never commit) ──
# CLAUDE.md = project context for Claude Code; .claude/ = local harness scaffolding
CLAUDE.md
.claude/

# ── Environment & secrets ──
# .env.local holds VPS_IP
.env
.env.*
*.local
*.secret
*.pem
id_rsa*

# ── Editor / IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── Logs ──
*.log
npm-debug.log*
